# docker-images
ships various base images with many tools pre-installed. These tools have licences on their own (debian, Android Tools, ...), so please make sure that you agree with them by downloading them, the repository owner does not own any of them.

the images strive to create a similar environment. They should be backwards compatible, so just use main